---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 11.0.2-jdk-stretch

inputs:
  - name: source

caches:
  - path: .gradle/

params:
  PUBSUB_SECRET:

run:
  path: source/pipeline/scripts/build.sh
